/**
 * Thin re-export so cat-pack owns its Lottie dependency path.
 * Plan phase may replace with vendored lottie-web.min.js copied into the module pack.
 */
export { default } from "https://cdn.jsdelivr.net/npm/lottie-web@5.12.2/+esm";
